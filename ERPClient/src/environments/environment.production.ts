export const environment = {
  production: true,
  // Istemciyle ayni origin'den servis edilir; /api Caddy tarafindan API'ye
  // yonlendirilir, boylece tarayicida cross-origin cagri olmaz.
  apiUrl: '/api',
};
